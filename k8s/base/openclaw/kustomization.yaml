apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# OpenClaw is a standalone component — included ONLY by overlays that opt in.
# It is NOT listed in k8s/base/kustomization.yaml (separation by design).

namespace: k8s-monitor

labels:
  - pairs:
      app.kubernetes.io/managed-by: kustomize
      app.kubernetes.io/component: openclaw
    includeSelectors: false

resources:
  - serviceaccount.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
