apiVersion: v1
kind: Service
metadata:
  name: openclaw
  labels:
    app: openclaw
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/part-of: k8s-daily-monitor
spec:
  type: ClusterIP
  selector:
    app: openclaw
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
