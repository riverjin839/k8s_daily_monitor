# ============================================
# OpenClaw - Messenger Credentials (placeholder)
# Replace values with base64-encoded real tokens before deploy.
# ============================================
apiVersion: v1
kind: Secret
metadata:
  name: openclaw-messenger-secret
  namespace: k8s-monitor
  labels:
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/part-of: k8s-daily-monitor
type: Opaque
stringData:
  # Telegram Bot token & chat ID
  TELEGRAM_BOT_TOKEN: ""
  TELEGRAM_CHAT_ID: ""
  # Slack Incoming Webhook URL
  SLACK_WEBHOOK_URL: ""
